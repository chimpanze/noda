{
  "id": "jwt-sign",
  "method": "POST",
  "path": "/api/jwt-sign",
  "summary": "util.jwt_sign — generate a signed JWT",
  "trigger": { "workflow": "jwt-sign", "input": { "uid": "{{ body.uid }}" } }
}
