{
  "id": "notify",
  "method": "POST",
  "path": "/api/feeds/:channel/notify",
  "summary": "Push a note to an SSE feed",
  "tags": ["realtime"],
  "trigger": {
    "workflow": "notify",
    "input": {
      "channel": "{{ params.channel }}",
      "text": "{{ body.text }}"
    }
  }
}
