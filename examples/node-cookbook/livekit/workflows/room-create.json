{
  "id": "room-create",
  "name": "Create Room",
  "nodes": {
    "create": {
      "type": "lk.roomCreate",
      "services": { "livekit": "lk" },
      "config": {
        "name": "{{ input.name }}",
        "empty_timeout": 600,
        "metadata": "{\"purpose\":\"cookbook\"}"
      }
    },
    "respond": {
      "type": "response.json",
      "config": {
        "status": 201,
        "body": "{{ nodes.create }}"
      }
    }
  },
  "edges": [
    { "from": "create", "to": "respond" }
  ]
}
