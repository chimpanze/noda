{
  "id": "send-data",
  "name": "Send Data",
  "nodes": {
    "send": {
      "type": "lk.sendData",
      "services": { "livekit": "lk" },
      "config": {
        "room": "{{ input.room }}",
        "data": { "kind": "cookbook-ping" },
        "topic": "cookbook"
      }
    },
    "respond": {
      "type": "response.json",
      "config": {
        "status": 200,
        "body": "{{ nodes.send }}"
      }
    }
  },
  "edges": [
    { "from": "send", "to": "respond" }
  ]
}
