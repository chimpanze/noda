{
  "id": "room-create",
  "method": "POST",
  "path": "/api/rooms",
  "summary": "lk.roomCreate — create a LiveKit room",
  "tags": ["livekit"],
  "trigger": {
    "workflow": "room-create",
    "input": {
      "name": "{{ body.name }}"
    }
  }
}
