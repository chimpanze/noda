{
  "id": "egress-start-track",
  "method": "POST",
  "path": "/api/egress/track",
  "summary": "lk.egress_start_track — start a single-track recording (dev server has no egress worker)",
  "tags": ["livekit"],
  "trigger": {
    "workflow": "egress-start-track",
    "input": {
      "room": "{{ body.room }}",
      "track_sid": "{{ body.track_sid }}"
    }
  }
}
