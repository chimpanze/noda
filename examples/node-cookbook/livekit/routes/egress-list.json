{
  "id": "egress-list",
  "method": "GET",
  "path": "/api/egress",
  "summary": "lk.egress_list — list egress recordings",
  "tags": ["livekit"],
  "trigger": {
    "workflow": "egress-list",
    "input": {
      "room": "{{ query.room }}"
    }
  }
}
