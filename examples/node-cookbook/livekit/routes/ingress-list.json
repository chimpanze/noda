{
  "id": "ingress-list",
  "method": "GET",
  "path": "/api/ingress",
  "summary": "lk.ingress_list — list ingress endpoints",
  "tags": ["livekit"],
  "trigger": {
    "workflow": "ingress-list",
    "input": {
      "room": "{{ query.room }}"
    }
  }
}
