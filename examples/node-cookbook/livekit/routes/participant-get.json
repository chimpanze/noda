{
  "id": "participant-get",
  "method": "GET",
  "path": "/api/rooms/:room/participants/:identity",
  "summary": "lk.participant_get — get a participant by identity (404 demo: no live participant exists)",
  "tags": ["livekit"],
  "trigger": {
    "workflow": "participant-get",
    "input": {
      "room": "{{ params.room }}",
      "identity": "{{ params.identity }}"
    }
  }
}
