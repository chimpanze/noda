{
  "id": "verify-credentials",
  "method": "POST",
  "path": "/api/auth/verify-credentials",
  "summary": "auth.verify_credentials — check email+password",
  "trigger": {
    "workflow": "verify-credentials",
    "input": { "email": "{{ body.email }}", "password": "{{ body.password }}" }
  }
}
