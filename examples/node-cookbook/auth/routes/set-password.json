{
  "id": "set-password",
  "method": "POST",
  "path": "/api/auth/set-password",
  "summary": "auth.set_password — consume a reset token and set a new password",
  "trigger": {
    "workflow": "set-password",
    "input": { "token": "{{ body.token }}", "password": "{{ body.password }}" }
  }
}
