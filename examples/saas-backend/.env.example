# Database
DATABASE_URL=postgres://noda:noda@localhost:5432/noda?sslmode=disable

# Redis (used for cache and streams)
REDIS_URL=redis://localhost:6379/0

# JWT
JWT_SECRET=change-me-in-production

# Email (SMTP)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=noda@example.com
