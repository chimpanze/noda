# AI-usability findings — 2026-06-25 re-run (0 confirmed gaps)

Generated by the AI-usability harness (`tools/ai-usability/harness.workflow.js`) running all five briefs against the live `noda mcp` server, **after** the doc/schema fixes from #223–#232 were merged (PR #234, commit `fb49f8e`).

Run: 12 agents, ~444k tokens, ~7m24s. Builders raised **7 candidate friction points**; an adversarial evaluator restricted to the MCP surface **refuted all 7** (`is_real_gap: false`). **0 confirmed findings, 0 issues.**

> Nothing to file. This is the measurement of progress, not a list of gaps.

## Progress vs. first run (2026-06-25)

| Run | Agents | Tokens | Candidates raised | Confirmed findings | Issues filed |
|-----|--------|--------|-------------------|--------------------|--------------|
| First (pre-fix) | 21 | ~844k | — | 11 | 10 (#223–#232) |
| Re-run (post-fix) | 12 | ~444k | 7 | **0** | 0 |

**11 → 0 confirmed findings.** The fixes closed the gaps. Fewer agents/tokens this run because the verify and synthesize stages had almost no surviving work.

## Candidates raised and why each was refuted

Builders still *hit* rough edges and dutifully logged them, but in every case the needed information was reachable through the MCP surface the fixes added — so the evaluator refuted the gap.

| Brief | # candidates | What the builder flagged | Why refuted |
|-------|--------------|--------------------------|-------------|
| 01-trivial | 0 | — | — |
| 02-basic | 2 | (verified, both refuted) | info reachable via MCP tools/resources |
| 03-data | 0 | — | — |
| 04-auth | 2 | (verified, both refuted) | info reachable via MCP tools/resources |
| 05-hard | 3 | `service_deps` casing on `db.create`; `noda_validate_config` per-node `config` not validated inline; WebSocket connections file location | `noda://docs/services` states the instance/plugin distinction; `noda_get_config_schema(workflow)` documents that node config validates at bootstrap; `noda://docs/realtime` states the connections file location in its first sentence |

The 05-hard refutals are the clearest signal: each candidate was answered by a resource or schema description introduced in #234 (`noda://docs/services`, `noda://docs/realtime`, the `workflow` config-schema note).

## Re-running

The gaps are closed for now. Re-run after any future MCP-surface or doc change to catch regressions; a non-zero confirmed count would mean a new gap was introduced.
