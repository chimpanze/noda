{
  "id": "drop-download",
  "method": "GET",
  "path": "/drops/:id/file",
  "summary": "Download a drop's file",
  "tags": ["drops"],
  "middleware": ["auth.session", "security.headers"],
  "trigger": {
    "workflow": "drop-download",
    "input": { "id": "{{ params.id }}" }
  }
}
