{
  "id": "drop-delete",
  "method": "DELETE",
  "path": "/drops/:id",
  "summary": "Delete a drop (and its file bytes)",
  "tags": ["drops"],
  "middleware": ["auth.session"],
  "trigger": {
    "workflow": "drop-delete",
    "input": { "id": "{{ params.id }}" }
  }
}
