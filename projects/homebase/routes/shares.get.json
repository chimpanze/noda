{
  "id": "share-get",
  "method": "GET",
  "path": "/s/:token",
  "summary": "View a shared drop (no auth)",
  "tags": ["public"],
  "middleware": ["limiter", "security.headers"],
  "trigger": {
    "workflow": "share-get",
    "input": { "token": "{{ params.token }}" }
  }
}
