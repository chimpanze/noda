{
  "id": "drop-get",
  "method": "GET",
  "path": "/drops/:id",
  "summary": "Get one drop",
  "tags": ["drops"],
  "middleware": ["auth.session"],
  "trigger": {
    "workflow": "drop-get",
    "input": { "id": "{{ params.id }}" }
  }
}
