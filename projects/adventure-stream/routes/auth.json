{
  "id": "auth-login",
  "method": "POST",
  "path": "/api/auth/login",
  "summary": "Admin login",
  "tags": ["auth"],
  "body": {
    "schema": { "$ref": "schemas/AdminLogin" }
  },
  "trigger": {
    "workflow": "admin-login",
    "input": {
      "password": "{{ body.password }}"
    }
  }
}
