{
  "id": "create-task",
  "method": "POST",
  "path": "/api/v1/tasks",
  "summary": "Create a new task",
  "tags": ["tasks"],
  "middleware": ["auth.jwt"],
  "body": {
    "schema": { "$ref": "schemas/Task" }
  },
  "trigger": {
    "workflow": "create-task",
    "input": {
      "title": "{{ body.title }}",
      "description": "{{ body.description }}"
    }
  }
}
