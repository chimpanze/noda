{
  "id": "get-task",
  "name": "Get Task",
  "nodes": {
    "fetch": {
      "type": "db.findOne",
      "services": { "database": "main-db" },
      "config": {
        "table": "tasks",
        "where": { "id": "{{ input.task_id }}" }
      }
    },
    "check": {
      "type": "control.if",
      "config": {
        "condition": "{{ nodes.fetch != nil }}"
      }
    },
    "respond-ok": {
      "type": "response.json",
      "config": {
        "status": 200,
        "body": "{{ nodes.fetch }}"
      }
    },
    "respond-not-found": {
      "type": "response.error",
      "config": {
        "status": 404,
        "code": "NOT_FOUND",
        "message": "Task not found"
      }
    }
  },
  "edges": [
    { "from": "fetch", "to": "check" },
    { "from": "check", "output": "then", "to": "respond-ok" },
    { "from": "check", "output": "else", "to": "respond-not-found" }
  ]
}
