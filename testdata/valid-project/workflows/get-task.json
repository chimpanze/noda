{
  "id": "get-task",
  "name": "Get Task",
  "nodes": {
    "fetch": {
      "type": "db.findOne",
      "services": { "database": "main-db" },
      "config": {
        "table": "tasks",
        "where": { "id": "{{ input.task_id }}" }
      }
    },
    "check": {
      "type": "control.if",
      "config": {
        "condition": "{{ fetch != nil }}"
      }
    },
    "respond-ok": {
      "type": "response.json",
      "config": {
        "status": 200,
        "body": "{{ fetch }}"
      }
    },
    "respond-not-found": {
      "type": "response.error",
      "config": {
        "status": 404,
        "message": "Task not found"
      }
    }
  },
  "edges": [
    { "from": "fetch", "to": "check" },
    { "from": "check", "output": "then", "to": "respond-ok" },
    { "from": "check", "output": "else", "to": "respond-not-found" }
  ]
}
